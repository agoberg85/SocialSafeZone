{
  "scripts": {
    "build": "mkdir public && cp -r *.html static admin public/ && echo \"window.supabaseUrl = '$SUPABASE_URL'; window.supabaseKey = '$SUPABASE_ANON_KEY';\" > public/static/config.js"
  },
  "devDependencies": {
    "supabase": "^2.53.6"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.76.1"
  }
}
